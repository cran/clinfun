c     G-rho family of tests for comparing survival distributions
      subroutine lrtest(n, p, nrisk, surv, irisk, sts, grp, lrnum,
     1     lrvar, rho)
      integer n, p
      double precision nrisk(n), surv(n), irisk(p), sts(n), grp(n),
     1     lrnum(p), lrvar(p,p), rho


      return
      end
